GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
# Optional: override the default SQLite path (defaults to ./data/saber.db)
SQLITE_PATH=
# JWT signing secret (auto-uses a dev fallback if unset)
JWT_SECRET=
